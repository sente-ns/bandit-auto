# bandit-auto
Automated solver for OverTheWire Bandit wargame (levels 0–15). Hardened SSH automation via paramiko: password redaction in logs, Tar-Slip protection, MITM-resistant host-key pinning, multi-strategy SSH key auth, TLS submission. All tunables via environment variables. Educational use only.
